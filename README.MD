# PlatformSheet

Allows you to colocate styles for both IOS an Android.


# Example

```
var styles = PlatformSheet.create({
    
    baseStyle: {
        backgroundColor: '#FFF'
    },
    androidbaseStyle = {
        backgroundColor: '#000'
    },
    iosbaseStyle = {
        backgroundColor: '#DDD'
    }

})